# DLSS5 一键开启工具（Go 原生版）安全修复说明（v2.1.0 / v2.1.1）

本版本依据《DLSS5一键开启工具源码安全审核报告》（2026-09-02）对全部 12 项发现（高危 2 · 中危 5 · 低危 5）及 3 条附加注记逐项修复。
版本号由 2.0.0 升级为 2.1.0（winres 资源同步更新）。

## 修复对照表

| 编号 | 等级 | 原问题 | 修复方式 | 涉及文件 |
|---|---|---|---|---|
| F-01 | 高 | 下载组件零完整性校验，`sha256sum` 为死代码 | 新增 `integrity.go`：**双层基准校验**。`pinnedSHA256` 固定基准（8 个在线构件的官方源实测 SHA-256）+ TOFU 运行期基准（`cache\integrity-baseline.json`，首次获取自动记录）。下载完成与缓存命中均强制校验，不符即删除并按失败处理（fail-closed）；`sha256sum` 调用链全部激活；`components\` 手动投递文件记录哈希供人工核对 | integrity.go（新）、core.go |
| F-02 | 高 | 移动目标下载源（mainline 分支 / latest release / 首页抓取） | 下载源**全部锁定**：Feeder `v0.11.0-beta.1`、LumeniteFX commit `76fa3e4d`、ReShade 安装器固定 6.8.0 直链、RHI `RHI-2.5.1`、dgVoodoo2 `v2.87.3`、头文件走 master 但由固定哈希拒收漂移。删除 `latestTag()`、`githubAssetURL()`、首页 HTML 抓取整套探测逻辑。升级须随工具版本发布同步更新常量与基准表 | fetch.go、core.go |
| F-03 | 中 | 静默执行第三方 RHI-Setup.exe，资产匹配正则过宽 | 资产名与 tag 双固定（不再用"任意 exe"正则）；新增 `confirmThirdPartyInstaller()`：GUI 模式执行前弹"是/否"确认框并明示社区镜像来源，拒绝则按组件缺失处理；静默模式记录 WARN 后放行 | fetch.go、core.go、ui.go |
| F-04 | 中 | PowerShell 脚本单引号拼接路径（含提权上下文）可注入 | 三处调用点（提权追加 ReShadeApps、桌面快捷方式、nvidia-smi 查询）全部改为**静态脚本文本 + 环境变量传参**（`$env:DLSS5_*`），脚本内不再出现任何拼接的路径；快捷方式脚本因含中文保留 UTF-8 BOM | install.go、envcheck.go、core.go |
| F-05 | 中 | 下载中途断流后不完整文件被当作有效缓存 | 读循环仅 `io.EOF` 判定成功，其余错误一律视为中断并删除临时文件重试；写盘错误即中止；落盘前与 `Content-Length` 比对，缺字节即失败；缓存命中也复验完整性，坏缓存自动删除重下 | core.go |
| F-06 | 中 | HTTP 客户端无超时，可永久挂起 | `ResponseHeaderTimeout: 30s` + `TLSHandshakeTimeout: 10s` + 请求级总超时 30 分钟（`context.WithTimeout`），任何环节挂起均会快速失败 | core.go |
| F-07 | 中 | 组件缺失硬闸门覆盖不全，关键文件静默缺失 | `installGameRoute` 安装前新增**路由级预检**：按目标位数/路由逐一硬校验 Feeder 本体、DLSS5_Feed.fx、Generic Depth、host64.exe、dgVoodoo2、ReShade 安装器，缺失即中止并列出清单；`getAllComponents` 补登记 `DLSS5_Feed.fx` 缺失；ReShade 交互安装后补扫游戏目录回收 Generic Depth | install.go、fetch.go |
| F-08 | 低 | 版本资源缓冲为包级全局变量，存在并发数据竞争 | 删除全局 `verBuf/verBufSize`，`verString` 改为接收本次调用私有缓冲，彻底消除共享状态；附加修复：VS_FIXEDFILEINFO 长度阈值 13 → 52（真实结构体大小）并校验签名 `0xFEEF04BD` | winver.go |
| F-09 | 低 | 卸载直接删合并键，可能删除用户原有配置 | 安装合并前备份原文件为 `*.dlss5bak`（ReShade.ini / ReShadePreset.ini / ReShadeApps.ini），备份名记入清单；卸载优先整体还原备份，仅旧版清单无备份时才退回删键；还原后的文件不再重复删键 | install.go、uninstall.go |
| F-10 | 低 | INI 合并依赖 map 遍历，键序不确定 | 新增 `sortedKeys()`，补键、节内替换、新建节三处全部按排序键序写入，多次执行产出字节级一致（含单测验证） | core.go |
| F-11 | 低 | ZIP 解压无体积上限，缺压缩炸弹防护 | 条目数 ≤4096、单条目 ≤256MB、解压总量 ≤1GB 三重限额；限额读取 + 实际字节数复核；路径穿越防护扩展为"拒绝 `..`、盘符、绝对路径 + 目标目录包含性双重校验"；解压错误不再静默跳过而是上抛 | core.go |
| F-12 | 低 | 导入表子串误报；死依赖 govaluate；lxn/walk 停止维护 | 导入名匹配加非单词字符边界正则（`myd3d11.dll` 类内嵌子串不再误报，含单测）；go.mod/go.sum 删除从未引用的 `gopkg.in/Knetic/govaluate.v3`；lxn/walk 仅为 UI 依赖暂无替代，已知风险保留并在此声明，计划后续评估迁移 | pe.go、go.mod、go.sum |

## 附加修复（审核报告注记项）

- **reshade_home.html 缓存无过期**：随 F-02 一并解决（首页抓取逻辑整体删除，不再产生该缓存文件）。
- **界面日志按字节中点截断可切碎 UTF-8**：`appendLog` 截断点对齐到 UTF-8 字符边界再截断（ui.go）。
- **ReShadeApps.ini 只清理首个 Apps= 行**：卸载改为逐行摘除本游戏、处理全部 `Apps=` 行，唯一登记时整行移除；优先走整体还原（F-09）（uninstall.go）。

## 修复过程中确认的隐藏功能缺陷（一并修复）

1. **Feeder 下载 URL 拼接必 404**：原代码按 `DLSS5-Feeder-<tag>.zip` 拼资产名，而官方资产名不带 v 前缀（如 `DLSS5-Feeder-0.11.0-beta.1.zip`），即下载主链路实际从未成功过。已按真实资产名规则拼接。
2. **generic_depth 上游分发已死**：`crosire/reshade-docs` 最新 release（v2026-08-02）已不再附带 `generic_depth.addon64/32`，原下载链必 404 且无闸门。现改为：`components\` 手动投递（记录哈希）→ 历史源探测 → 明确报缺；ReShade 交互安装附带安装时自动补扫回收。

## 固定版本与 SHA-256 基准（2026-09-02 实测）

| 缓存文件 | 锁定源 | SHA-256 |
|---|---|---|
| DLSS5-Feeder.zip | github.com/jlrouzies-fr/DLSS5-Feeder `v0.11.0-beta.1`（资产 `DLSS5-Feeder-0.11.0-beta.1.zip`） | `516899f38a978b9c58d107667102439296e79868173e8be01501c9d083be3a87` |
| LumeniteFX.zip | codeload @ commit `76fa3e4d601c97e9bc63f119c01405b7b9938885` | `bf574543a6af6527587af0bad139922e8c0363bb154cdfb3e41133c7dca2ee3f` |
| ReShade.fxh | crosire/reshade-shaders master（哈希锚定） | `6dabfbbaf968c3871905d2ea17f96572ff7b1cec01310b5d0e5252b66b30174f` |
| ReShadeUI.fxh | 同上 | `78adf672df47460297eb9fe6dd238d2aafa24510b52b84feb1a745dff70eb901` |
| DrawText.fxh | 同上 | `b79cc4dfb3e98bcf4c06193d00ea7631d74f467f73a4deeeee13e71336d3e680` |
| ReShade_Setup_6.8.0_Addon.exe | reshade.me 官方直链 | `afe4c8f13048306307983b8b3d41d5bf00a86820440b0e57dea10950e1176445` |
| RHI-Setup.exe | github.com/RankFTW/RHI `RHI-2.5.1`（解包产物确定性由此锚定） | `3eadcc03af8b3ba7ec277339bc0a9fb51077d5dc0781bba94790aa6e70864bc2` |
| dgVoodoo2.zip | github.com/dege-diosg/dgVoodoo2 `v2.87.3`（资产 `dgVoodoo2_87_3.zip`） | `6fb954bed55bf70e948c5045a663a9df31ea206faf105e327bafe46c318f867f` |
| generic_depth.addon64/32 | 上游已停止分发，手动投递 `components\`，TOFU 基准 | —— |

## 升级组件的流程

1. 确认新版本 tag/commit 与资产名；
2. 从官方源下载并用 `sha256sum`（或 `certutil -hashfile <file> SHA256`）记录基准；
3. 更新 `fetch.go` 中的 `pin*` 常量与 `integrity.go` 中的 `pinnedSHA256`；
4. 版本号（`core.go` 的 `toolVersion` 与 `winres/winres.json`）同步递增，随工具版本发布。

## 构建与测试

- 本地构建：`go build -trimpath -ldflags "-s -w -H windowsgui" .`（Windows，Go ≥ 1.22；版本资源由仓库内 `rsrc_windows_amd64.syso` 提供，修改 winres 后需 `go-winres make` 重新生成）
- 单元测试：`go test -v ./...`（6 用例：INI 键序确定性、zip 穿越与正常解压、TOFU/固定基准校验、导入表边界正则；v2.1.2 起可在任意平台执行）
- 持续集成：`.github/workflows/build.yml`（windows-latest：vet → test → build）
- 局限说明（v2.1.0 原文，v2.1.1 起已消除）：v2.1.0 产出环境无 Go 工具链未能本地编译验证；v2.1.1/2.1.2 均已在真实工具链中完成构建、vet 与测试执行。

## v2.1.1 修订（2026-09-02，由复验方在真实工具链中完成）

以下问题由复验方（本工具原作者侧）在实测中发现并修正，v2.1.1 为可直接构建发布的版本：

1. **恢复依赖 gopkg.in/Knetic/govaluate.v3（关键修正）**：v2.1.0 的 F-12 将其误判为死依赖删除，实际它是 `lxn/walk` 的传递依赖（walk 的 expression.go / declarative/builder.go 在用）。删除后默认 readonly 模式（CI 与本地构建均默认此模式）构建必失败：
   `no required module provides package gopkg.in/Knetic/govaluate.v3`。
   v2.1.1 已恢复为 indirect 依赖，go vet / go test 编译 / go build 全部验证通过。
2. **全量 gofmt**：v2.1.0 因产出环境无 Go 工具链，全部 .go 文件缩进被改为空格、不符合 gofmt；v2.1.1 已用 Go 1.22.5 gofmt 全量格式化（gofmt -l 零输出）。
3. **expandZip 跳过条目补充日志**：路径穿越/逃逸目标目录的条目原为静默跳过，现记录 WARN 日志便于排查。
4. **版本资源对齐**：v2.1.0 的 winres/winres.json 虽已改为 2.1.0，但预编译 .syso 未重新生成（exe 属性页仍为 2.0.0）；v2.1.1 已用 go-winres 重新生成，属性页与版本号一致（2.1.1.0）。

## v2.1.2 修订（2026-09-02，CI 实测发现）

v2.1.1 推送 CI 后，`TestIniSetKeysDeterministic` 在真实运行中失败（v2.1.0/2.1.1 期间测试只做过 Windows 下的编译校验，未在本地执行过），暴露一处运行时缺陷，本版修复：

1. **INI 读回改写时 UTF-8 BOM 逐次累积（关键修正）**：`writeTextBOM` 每次写入都在文件头补一个 UTF-8 BOM，而 `iniSetKeys`/`iniRemoveKeys`/`ensureReshadeAppsEntry`（ReShadeApps.ini 登记）/卸载名单摘除四处"读回→改写"链路读取时不剥离旧 BOM，导致每保存一次文件头就多一个 BOM（`\ufeff\ufeff\ufeff…`）。实测影响：重复执行"一键开启/一键卸载"后 ReShade.ini、ReShadePreset.ini 头部堆积 BOM；ReShade 的 INI 解析对 BOM 宽容故通常不影响功能，但属于明确的配置文件污染。修复：新增 `stripBOM()`（含历史累积的多个 BOM 一并剥离），四处读回改写统一先剥离再处理；首次读入的产物从此字节级稳定（`TestIniSetKeysDeterministic` 现已通过）。
2. **单元测试可跨平台执行**：`ui.go`（lxn/walk）与 `winver.go`（version.dll/syscall）为 Windows 专属，导致 `go test ./...` 此前只能在 Windows 上运行（本缺陷因此漏网到 CI）。现两文件加 `//go:build windows` 标签，新增 `stub_other.go` 为其他平台提供 `runGUI`/`isReShadeDll`/`isDxvkDll` 空实现；Linux/macOS 可直接 `go test ./...`、`go vet ./...` 本地回归，Windows 构建不受影响（两个 Windows 文件照常编译）。
3. **版本号 2.1.2**：因 2.1.1 哈希已对外公示（整包 `3b9a523c95dcdcf0` / exe `29243adccfa64037`），本版为运行时行为修正，按语义递增为 2.1.2，winres 资源同步重新生成（属性页 2.1.2.0）。

本版验证矩阵（Go 1.22.5）：`gofmt -l` 零输出；`go test -v ./...` 6/6 通过（Linux 实际执行）；`GOOS=windows go vet` 通过；`GOOS=windows GOARCH=amd64 go build -trimpath -ldflags "-s -w -H windowsgui"` 产出 9,246,208 字节 exe。
